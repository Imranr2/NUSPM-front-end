import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  top: {
    marginTop: "150px",
  },
  button: {
    borderRadius: 30,
    alignItems: "center",
    marginTop: 24,
    marginBottom: 16,
  },
}));

export { useStyles };
