import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  button: {
    borderRadius: 30,
    alignItems: "center",
    marginTop: 24,
    marginBottom: 16,
  },
  alert: {
    marginBottom: 16,
  },
}));

export { useStyles };
